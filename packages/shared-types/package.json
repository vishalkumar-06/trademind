{
  "name": "@trademind/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types imported by every service: AgentResult, ExecutionPlan, IngressEvent, AgentType, MCP contracts.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
