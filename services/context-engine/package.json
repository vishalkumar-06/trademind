{
  "name": "@trademind/context-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1: Persistent Context Engine — PostgreSQL schema, ContextService, and REST API",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "node --loader ts-node/esm src/server.ts",
    "start": "node dist/server.js",
    "migrate": "node --loader ts-node/esm src/migrations/run.ts",
    "lint": "tsc --noEmit",
    "test": "echo 'TODO: add tests'"
  },
  "dependencies": {
    "@trademind/shared-types": "*",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "pgvector": "^0.2.0",
    "zod": "^3.23.8",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "@types/pg": "^8.11.2",
    "ts-node": "^10.9.2"
  }
}
